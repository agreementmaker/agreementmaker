# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

GET		/initServer							controllers.Application.initServer()

# A user registers a session.
GET		/listTasks							controllers.Application.listTasks()
GET		/register/*taskid					controllers.Application.register(taskid)
GET		/getCandidateMapping/*userid		controllers.Application.getCandidateMapping(userid)
GET		/setFeedback/*mappingid/*feedback	controllers.Application.setFeedback(mappingid,feedback)

GET		/listOntologies						controllers.Application.listOntologies()
GET		/showMappings						controllers.Application.showMappings()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

